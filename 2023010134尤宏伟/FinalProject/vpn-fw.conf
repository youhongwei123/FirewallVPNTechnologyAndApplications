# vpn-fw.conf — fw端WireGuard配置
# 注意：实际使用时请用 vpn-setup.sh 自动生成（包含真实密钥）
# 以下为模板示例

[Interface]
# fw 端 VPN 接口
Address = 10.10.10.1/24
PrivateKey = <FW_PRIVATE_KEY>
ListenPort = 51820

[Peer]
# remote 客户端
PublicKey = <REMOTE_PUBLIC_KEY>
AllowedIPs = 10.10.10.2/32
PersistentKeepalive = 25
